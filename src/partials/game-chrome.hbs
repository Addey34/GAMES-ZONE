<!--
  Chrome shared by every game: just the global sidebar now.
  The leaderboard lives in the shell's "Leaderboard" panel (shell-open) for the
  games that enable it (`leaderboard: true`), and game-over goes through the
  JS-managed overlay (src/shared/ui/gameOverlay.ts) — no modal here.
  Navigation (back home + other games) lives in the global sidebar.
-->
{{> sidebar}}
